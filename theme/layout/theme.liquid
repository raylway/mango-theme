<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {%- render 'global-head' -%}

    {{ content_for_header }}
  </head>
  <body>
    <main role="main">
      {{ content_for_layout }}
    </main>
  </body>
</html>

